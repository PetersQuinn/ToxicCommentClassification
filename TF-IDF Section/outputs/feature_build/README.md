# Feature Build

This folder stores TF-IDF feature diagnostics from `step_2_build_tfidf_features.py`.

For the GitHub-facing version, we keep only `tfidf_build_metadata.json`. The larger candidate tables and vectorizer artifact stay local.
