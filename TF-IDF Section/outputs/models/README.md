# Models

The current TF-IDF section keeps three model families:

- `logistic_regression`
- `linear_svm`
- `complement_nb`

Their full tuning, evaluation, plot, and export artifacts are useful locally but too noisy for the GitHub-facing version of this section, so we keep those generated files out of version control.
