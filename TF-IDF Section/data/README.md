# Data

We keep the cleaned Jigsaw parquet files here when we run this TF-IDF section locally.

Expected files:

- `parquets/train_cleaned.parquet`
- `parquets/test_cleaned.parquet`
- `parquets/test_labeled_cleaned.parquet`

These inputs are required for the pipeline, but we do not track them in GitHub.
