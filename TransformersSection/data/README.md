# Data

I keep the transformer-side working copies of the project data here.

What matters
- `parquets/` is where I staged the cleaned parquet files I used for the notebook and the Colab runs.
- These are local working copies for this section, not the version-controlled story I want GitHub to carry.

I keep this folder documented in Git, but I leave the actual large data files out because they make the repo heavy and duplicate data that belongs elsewhere in the bigger project.
