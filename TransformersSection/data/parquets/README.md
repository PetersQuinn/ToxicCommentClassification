# Parquets

I used this folder for local parquet copies that feed the transformer notebook and the GPU-backed runs.

The main files I kept here were:
- `train_cleaned.parquet`
- `test_cleaned.parquet`
- `test_labeled_cleaned.parquet`
- `dense_engineered_train.parquet`
- `dense_engineered_test.parquet`

I leave the actual parquet files out of Git because they are large local inputs, not the useful review artifacts for this transformer section.
