# Raw Data

We keep the original Jigsaw CSV files here locally so `run_option_a.py` can find them.

Expected files:
- `train.csv`
- `test.csv`
- `test_labels.csv`
- `sample_submission.csv`

We do not track those files in Git because they are raw dataset assets, not section code.
