# Source Code

We keep only the modules needed for this section:
- `data_io/` loads and validates the raw CSV files
- `preprocessing/` normalizes comment text for analysis and feature export
- `eda/` writes summary tables and figures
- `features/` builds dense features, TF-IDF exports, and lexicon-based diagnostics
- `utils/` holds config, output helpers, and runtime checks
